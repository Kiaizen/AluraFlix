{
  "version": 2,
  "builds": [
    {
      "src": "src/index.html",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build && json-server --watch db.json"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/*",
      "dest": "json-server"
    }
  ]
}